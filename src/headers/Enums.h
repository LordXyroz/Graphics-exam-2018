#pragma once

// Enum for textbox allignment
enum Allignment
{
    center,
    left,
    right
};

enum CameraMode
{
    freeCam = 0,
    staticCam = 1,
    followCam = 2,
    trackingCam = 3,
    orbitTargetCam = 4,
    orbitPositionCam = 5
};